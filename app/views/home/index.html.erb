<div class="ui container">
  <% if user_signed_in? %>
    <h1 id="ld-headline" class="ui header">
      Hallo <%= current_user.email %> <br>
      willkommen in deinem persönlichen Bereich von Luftalarm!
    </h1>
  <% else %>
    <h1 id="ld-headline" class="ui header">
      Willkommen beim Luftalarm von Luftdaten.info
    </h1>
    <p>
        Egal ob Zuhause, bei der Arbeit oder deinem tagtäglichen Fahrweg. Hier kannst Du individuelle Benachrichtigungen über Sensor-Werte einstellen.
    </p>
      <%= link_to('Registrierung', new_user_registration_path, class: 'ui button', id:'ld-primary')  %>
      <%= link_to('Login', new_user_session_path, class: 'ui button')  %>
  <% end %>
</div>

<h4 id="primary" class="ui horizontal divider header">
  <i class="tag icon"></i>
  Weiter gehts mit dem Content aller Art...
</h4>

<div class="ui column stackable middle aligned centered color grid">
    <a href="https://luftdaten.info" class="ui medium image" target="_blank">
      <img src="/static/images/feinstaub-sensor.jpg">
    </a>
</div>


<!-- <div class="ui basic segment" id="own_sensors">
<% if current_user.blank? %>
  <%# <h2>Logge dich ein und erstelle deinen persönlichen Sensoralarm</h2> %>
  <div id="map"></div>
<% else %>
  <%= simple_form_for Notification.new(user_id: current_user.id), html: { class: "ui form"} do |f| %>
    <div class="ui sub header">Titel des Sensoralarms:</div>
    <div class="one column row">
        <div class="column">
        <%= f.input_field :name, required: true %>
        </div>
    </div><br>
    <div class="one column row">
      <div class="ui sub header">Wähle die Sensoren auf der Karte:</div>
      <select id="sensor_select" class="ui fluid dropdown" multiple="" name="notification[sensor_ids][]" required="required">
      </select>
    </div><br>
    <div class="one column row">
        <div class="left aligned column">
          <%= f.button :button, '<i class="fa-send icon"></i>Speichern'.html_safe, type: :submit, class:"ui save_button positive button"%>
        </div>
    </div>
  <% end %>
</div>
<div class="ui basic segment" id="own_sensors">
  <div id="map"></div>
</div>
<% end %> -->

