<div class="ui container">
  <div class="ui basic segments">
    <div class="panel panel-default panel-boundary">
      <div class="panel-heading">
        <h2><%= t('.forgot_password', :default => 'Forgot your password?') %></h2>
      </div>
      <div class="ui formy">
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, role: "form" }) do |f| %>
            <%= devise_bootstrap_error_messages! %>
            <div class="field">
              <%= f.label :email %>
              <%= f.email_field :email, autofocus: true, class: "form-control" %>
            </div>
            <%= f.submit  t('.forgot_password_submit', :default => "Send me reset password instructions"), class: "ui primary basic button" %>
        <%end%>
      </div>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>